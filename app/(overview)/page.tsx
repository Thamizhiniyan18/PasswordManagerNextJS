export default async function Home() {
  return (
    <main className="">
      {/* {passwords.map((password, index) => (
        <div key={index}>{password.user_id}</div>
      ))} */}
      Home
    </main>
  );
}
